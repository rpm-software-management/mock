language: python

matrix:
  include:
    - python: "3.5"
    - python: "3.6"
    - python: "3.7"
    - python: "3.8"
      dist: xenial

install:
  - "pip install -r mock/requirements.txt"

script: PYTHON=python$TRAVIS_PYTHON_VERSION ./mock/run-tests.sh
