A new plugin to pre-process spec files with rpmautospec [has been implemented](PR#1253).

If this plugin is enabled, mock pre-processes spec files that use rpmautospec features (for
automatic release numbering and changelog generation) before building a source RPM.
