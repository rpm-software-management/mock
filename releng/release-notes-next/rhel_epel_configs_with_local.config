RHEL+EPEL configuration files now provide [local] repository pointing at EPEL
buildroots in Koji, so users can use `--enablerepo=local`.  These configs have
`gpgcheck=0` explicit, as implicit configuration for RHEL base is `gpgcheck=1`.
